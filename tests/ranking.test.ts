/*
import { Ranking } from "../src/ranking"
import { GithubRepository } from "../src/github_repository"

describe('Ranking Module', () => {
    // prelimnary tests, unrun due to relying on functionality not merged yet
    test('Give list of sample urls', () => {

        var repolist:GithubRepository[];
        repolist = [new GithubRepository("https://github.com/cloudinary/cloudinary_npm", "", "", ""), 
                    new GithubRepository("https://www.npmjs.com/package/express", "", "", ""), 
                    new GithubRepository("https://github.com/nullivex/nodist", "", "", ""), 
                    new GithubRepository("https://github.com/lodash/lodash", "", "", ""), 
                    new GithubRepository("https://www.npmjs.com/package/browserify", "", "", "")];
        
        var rank = new Ranking;
        rank.display_ranking(repolist); // nothing returned, just console output, maybe do a string return of that?
    })
})
*/